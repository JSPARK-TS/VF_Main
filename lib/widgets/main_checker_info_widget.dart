import 'package:flutter/material.dart';

class MainCheckerInfo extends StatelessWidget {
  const MainCheckerInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class MainConfigCon extends StatelessWidget {
  const MainConfigCon({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
